# Режим mock: запросы направляются на Prism (npm run dev:mock), порт 4010.
VITE_API_BASE_URL=http://localhost:4010
# Локальные моки фронтенда (frontend/src/api/queries.mock.ts) вместо реального API.
VITE_USE_MOCKS=true
