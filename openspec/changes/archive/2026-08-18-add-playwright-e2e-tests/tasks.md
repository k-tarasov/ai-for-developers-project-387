## 1. Инфраструктура e2e

- [x] 1.1 Добавить `playwright` как devDependency в `frontend/package.json` и установить браузеры (`npx playwright install`).
- [x] 1.2 Создать `frontend/playwright.config.ts` с `webServer` для фронта и бэкенда, `baseURL`, таймаутами и `reuseExistingServer`.
- [x] 1.3 Добавить npm-скрипт `e2e` в `frontend/package.json` и цель `make e2e` в корневом Makefile.
- [x] 1.4 Создать `frontend/e2e/fixtures.ts` (логин владельца + cookie `owner_session`, seed типа события, вычисление свободного слота).

## 2. Гостевой поток (guest.spec.ts)

- [x] 2.1 G1.1–G1.3: список типов событий, пустое состояние, ошибка загрузки.
- [x] 2.2 G2.1a–G2.10: профиль гостя (новый/знакомый, `rememberMe`, валидация контакта, редактирование карточки, persistence после перезагрузки, ошибки загрузки/создания профиля).
- [x] 2.3 G3.1–G3.3: просмотр слотов, пустые слоты, `EVENT_TYPE_NOT_FOUND`.
- [x] 2.4 G4.1–G4.5: создание брони, валидация контакта, `SLOT_BUSY`, ошибки валидации слота, комментарий к брони.

## 3. Владельческий поток (owner.spec.ts)

- [x] 3.1 O1.1–O1.4: успешный вход, неверные данные, лимит попыток, вход по существующей cookie.
- [x] 3.2 O2.1–O2.2: шапка с двумя кнопками, элементы управления владельца после входа.
- [x] 3.3 O3.1–O3.7: CRUD типов событий (создание, редактирование, удаление), дубликат slug, валидация, отсутствие сессии, собственное расписание доступности.
- [x] 3.4 O4.1–O4.4: просмотр/сохранение расписания, ошибка валидации расписания, 401 без сессии владельца.
- [x] 3.5 O5.1–O5.3: список предстоящих броней, пустое состояние, 401 без сессии.

## 4. Интеграция (integration.spec.ts)

- [x] 4.1 X1: гость создаёт бронь → владелец видит её в списке предстоящих.
- [x] 4.2 X2: владелец удаляет тип события с бронью → бронь сохраняется, новые брони невозможны (404).
- [x] 4.3 X3 (optional / destructive): перезапуск бэкенда очищает in-memory данные. Выполняется изолированно.

## 5. Проверка

- [x] 5.1 Запустить `make e2e` / `npm run e2e` — все сценарии зелёные.
- [x] 5.2 Прогнать линтер фронтенда (`make lint` / `npm run lint`).
