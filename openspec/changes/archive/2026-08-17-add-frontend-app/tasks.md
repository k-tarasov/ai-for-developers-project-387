## 1. Скелет фронтенда

- [x] 1.1 Создать `frontend/` через Vite (шаблон react-ts), проверить dev-сервер и сборку
- [x] 1.2 Подключить Tailwind CSS и инициализировать shadcn/ui (components.json, базовая тема)
- [x] 1.3 Установить shadcn-компоненты по списку: button, card, form, input, textarea, label, select, calendar, dialog, alert, badge, skeleton, table, tabs
- [x] 1.4 Настроить ESLint/Prettier для `frontend/` и добавить цели `lint`/`test`/`build` в корневой `Makefile`
- [x] 1.5 Зарегистрировать shadcn MCP Server в конфигурации агентов (`.opencode/`)

## 2. API-клиент по контракту

- [x] 2.1 Добавить скрипт генерации TypeScript-типов из OpenAPI (`spec/tsp-output`) через openapi-typescript
- [x] 2.2 Реализовать единственную точку доступа к API на openapi-fetch: базовый URL из `VITE_API_BASE_URL`, без обращений к fetch вне этого модуля
- [x] 2.3 Реализовать слой разбора ошибок 400/404/409: маппинг `code` из контракта на типизированные ошибки UI
- [x] 2.4 Покрыть unit-тестами маппинг ошибок (все коды: `VALIDATION_ERROR`, `CONTACT_REQUIRED`, `SLOT_MISALIGNED`, `SLOT_OUT_OF_WINDOW`, `SLOT_OUTSIDE_SCHEDULE`, `EVENT_TYPE_NOT_FOUND`, `DUPLICATE_EVENT_TYPE_ID`, `SLOT_BUSY`) и поведение при сетевой ошибке

## 3. Окружение: dev-прокси и mock-режим

- [x] 3.1 Настроить dev-прокси Vite `/api` → `http://localhost:8080` и `.env`-конфигурацию `VITE_API_BASE_URL`
- [x] 3.2 Добавить Prism как dev-зависимость и npm-скрипт `dev:mock` (Prism `mock` над OpenAPI-файлом + Vite на него)
- [x] 3.3 Проверить, что основные GET-сценарии фронтенда воспроизводятся против Prism без бэкенда

## 4. Гостевой сценарий

- [x] 4.1 Подключить React Router и TanStack Query; базовый layout с навигацией гость/владелец
- [x] 4.2 Страница `/` — список типов событий (название, описание, длительность), состояния загрузки/пусто/ошибка с повтором
- [x] 4.3 Страница `/book/:eventTypeId` — слоты по дням в окне записи (UTC с пометкой), пустое состояние, обработка 404 `EVENT_TYPE_NOT_FOUND`
- [x] 4.4 Форма бронирования: имя, телефон, email, комментарий; клиентская проверка «хотя бы один контакт» до запроса
- [x] 4.5 Создание брони через `POST /bookings`: подтверждение с данными брони; обработка `SLOT_BUSY` (сообщение + инвалидация слотов) и 400-кодов слота

## 5. Сценарии владельца

- [x] 5.1 Страница `/admin/event-types` — список, создание, редактирование, удаление (с подтверждением) типов событий; обработка `DUPLICATE_EVENT_TYPE_ID` и `VALIDATION_ERROR`
- [x] 5.2 Редактор собственного расписания доступности (`availability`) типа события в форме типа события
- [x] 5.3 Страница `/admin/schedule` — просмотр и полная замена расписания по умолчанию (неделя, интервалы HH:mm, пустой день = недоступен), обработка 400
- [x] 5.4 Страница `/admin/bookings` — предстоящие брони по возрастанию начала: гость, контакты, тип события, время; пустое состояние

## 6. Проверки и DoD

- [x] 6.1 Тесты: рендер страниц с mock-клиентом, сценарии ошибок по кодам, клиентская валидация контакта
- [x] 6.2 Пройти `make lint`, `make test`, `make build` — все зелёные
- [x] 6.3 Ручной сценарий против Prism: список типов → слоты → создание брони; админка: CRUD типов, расписание, список броней
- [x] 6.4 Обновить README: как запустить frontend (dev, dev:mock, build), требуемые env-переменные
