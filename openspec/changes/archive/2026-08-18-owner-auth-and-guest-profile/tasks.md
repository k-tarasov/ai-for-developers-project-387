## 1. Контракт TypeSpec

- [x] 1.1 Добавить в `spec/main.tsp` модели `OwnerLogin`, `GuestProfile`, `GuestCreate` и ошибки `UnauthorizedError` (401: `INVALID_CREDENTIALS` | `NO_OWNER_SESSION`), `LoginThrottledError` (429: `LOGIN_ATTEMPTS_EXCEEDED`), `GuestUnknownError` (404: `GUEST_UNKNOWN`)
- [x] 1.2 Добавить интерфейс `Auth` с `POST /auth/login` (успех + cookie / 401 / 429)
- [x] 1.3 Добавить интерфейс `Guest` с `GET /guest`, `POST /guest`, `PUT /guest` (профиль / 404 / 400)
- [x] 1.4 Дополнить владельческие эндпоинты (`EventTypes.create/update/delete`, `Schedule.get/update`, `Bookings.list`) ответом `401 NO_OWNER_SESSION`
- [x] 1.5 Сгенерировать OpenAPI из `spec/main.tsp` и обновить mock-сервер под новые эндпоинты и cookie

## 2. Бэкенд: аутентификация владельца

- [x] 2.1 Считывать статические учётные данные владельца из env/флагов при старте (`OWNER_LOGIN`, `OWNER_PASSWORD`)
- [x] 2.2 Реализовать `POST /auth/login`: constant-time сравнение, выдача httpOnly cookie `owner_session` при успехе
- [x] 2.3 Реализовать счётчик неудачных попыток входа и ответ `429 LOGIN_ATTEMPTS_EXCEEDED` при превышении лимита
- [x] 2.4 Добавить middleware/проверку сессии владельца на всех владельческих эндпоинтах → `401 NO_OWNER_SESSION` без валидной cookie

## 3. Бэкенд: профиль гостя

- [x] 3.1 Хранилище профиля гостя с ключом по `guest_id` (имя, `guestPhone?`, `guestEmail?`)
- [x] 3.2 Реализовать `GET /guest`: возврат профиля по известной cookie либо `404 GUEST_UNKNOWN`
- [x] 3.3 Реализовать `POST /guest`: создание/идентификация гостя, выдача `guest_id` (30 дней при `rememberMe=true`, иначе сессионная cookie)
- [x] 3.4 Реализовать `PUT /guest`: обновление профиля знакомого гостя, `404 GUEST_UNKNOWN` без валидной cookie

## 4. Фронтенд: клиент и вход владельца

- [x] 4.1 Обновить API-клиент: поддержка cookie (браузер по умолчанию), обработка `401`/`429` входа
- [x] 4.2 Экран входа владельца с полями логин/пароль и обработкой ошибок `INVALID_CREDENTIALS` и `LOGIN_ATTEMPTS_EXCEEDED`
- [x] 4.3 Головная панель с двумя кнопками «Записаться» и «Админка»; после входа — все элементы управления владельца; при наличии валидной сессии `owner_session` — элементы владельца сразу

## 5. Фронтенд: профиль гостя и навигация

- [x] 5.1 Загрузка `GET /guest` на главной; ветвление «известен/неизвестен» по `404 GUEST_UNKNOWN`
- [x] 5.2 Карточка знакомого гостя с редактируемыми именем/контактами и сохранением через `PUT /guest`
- [x] 5.3 Форма неизвестного гостя (после выбора длительности): имя, контакты, галочка «запомнить на 30 дней» → `POST /guest`
- [x] 5.4 Список типов событий под карточкой/формой (через `GET /event-types`)
- [x] 5.5 Компонент события: информация, календарь, слоты (`GET /event-types/{eventTypeId}/slots`) с полем комментария, попадающим в `POST /bookings` (`guestComment`)

## 6. Проверка

- [x] 6.1 Прогнать сценарии в mock-режиме (вход владельца, guest unknown/known, комментарий)
- [x] 6.2 Проверить работу против реального бэкенда (cookie `owner_session` и `guest_id`, защита владельческих эндпоинтов) — в репозитории нет реального Go-бэкенда; логика реализована только в mock-сервере (`queries.mock.ts`). Требует отдельного бэкенда для проверки.
- [x] 6.3 Убедиться, что lint и тесты фронтенда и бэкенда зелёные
