## 1. Подготовка данных для календаря

- [x] 1.1 В `book-page.tsx` построить из `slots` множество доступных дней через `groupSlotsByDay` + `utcDayKey` (UTC-полночь).
- [x] 1.2 Сформировать массив матчеров `disabled` для `Calendar`: границы окна (`windowStartsOn`–`windowEndsOn`) и дни вне множества доступных.

## 2. Двухколоночный макет выбора времени

- [x] 2.1 Добавить состояние `selectedDay: Date | undefined`, инициализировать первым доступным днём; сбрасывать `selectedSlot` при смене дня, если слот не принадлежит новому дню.
- [x] 2.2 Заменить секцию плоского списка слотов на трёхколоночный `grid gap-4 md:grid-cols-3`: (1) информация о событии, (2) календарь, (3) колонка слотов.
- [x] 2.3 Настроить `Calendar`: `mode="single"`, `selected`, `onSelect`, `defaultMonth={windowStartsOn}`, `captionLayout="dropdown"` (переключение месяцев и годов), `disabled`, локализация ru.
- [x] 2.4 В третьей колонке показывать закреплённый заголовок выбранного дня (день недели + число + «UTC») и вертикальный `space-y-2` из кнопок-слотов дня (по возрастанию, `formatUtcTime`), выделяя `selectedSlot`; обеспечить `overflow-y-auto` с ограниченной высотой, чтобы прокручивалась только эта колонка.

## 3. Тесты и проверка

- [x] 3.1 Адаптировать `book-page.test.tsx`: выбор дня в календаре перед появлением слотов; проверить, что дни без слотов неактивны.
- [x] 3.2 Убедиться, что сценарии создания брони и `SLOT_BUSY` проходят с новой раскладкой.
- [x] 3.3 Запустить линтер и тесты фронтенда (`npm run lint`, `npm test` / через `Makefile`) и добиться зелёного статуса.
